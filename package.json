{
  "scripts": {
    "dist": "rm -f -r dist && mkdir dist && cp -r node_modules/swagger-ui-dist dist/ && sed -i'.bak' 's#https://petstore.swagger.io/v2/swagger.json#swagger.yaml#g' dist/swagger-ui-dist/index.html && cp swagger.yaml dist/swagger-ui-dist/"
  },
  "dependencies": {
    "swagger-editor-dist": "^4.2.0",
    "swagger-ui-dist": "^4.6.2"
  }
}
